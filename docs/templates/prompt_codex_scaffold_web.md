# Prompt — Scaffold Web Gestão (React/Next)

Tarefa: criar o esqueleto do web admin (gestão) em `workspaces/web/`.

Entregas:
1) Projeto React/Next com rotas básicas
2) Tela de login (Google OAuth via backend — stub no MVP)
3) Página “Dashboard” consumindo `/api/v1/health`
4) Estrutura de pastas: pages, components, services/api, auth
5) README de execução local

DoD:
- `npm run dev` ok
- request ao health funcionando
