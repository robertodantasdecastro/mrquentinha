# Prompt - Iniciar Sessao (W10)

Execute o workflow `W10_iniciar_sessao.md`.

Regras obrigatorias:
- Ler `AGENTS.md` + memoria oficial (`PROJECT_STATE`, `DECISIONS`, `CHANGELOG`, `RUNBOOK_DEV`).
- Sem segredos em codigo, docs ou commit.
- Validar baseline tecnico (test/build) antes de iniciar desenvolvimento.
- Usar validador + git: mostrar comandos executados e status final.
